name: Visual Regressions Test

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  visual-regression:
    uses: cloudscape-design/actions/.github/workflows/visual-regression.yml@visual-regression
    with:
      repository: ${{ github.repository }}
    secrets: inherit
